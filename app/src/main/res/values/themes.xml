<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base theme for a Compose app: no ActionBar, colors handled in Compose. -->
    <style name="Theme.FrontekReads" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:statusBarColor">@color/brand_dark</item>
        <item name="android:windowBackground">@color/brand_cream</item>
    </style>
</resources>
